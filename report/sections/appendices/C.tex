% =============================================================
%  APPENDIX C — REWEIGHTING PIPELINE
% =============================================================

\section{Reweighting Pipeline}
\label{app:residual_impl}

This section describes the full implementation of ResiDual$^*$ (RD$^*$\xspace)
integrated into the frozen HTS-AT transfer of CLAP model.
The pipeline consists of two sequential phases: a \emph{fitting pass}
that estimates a fixed PCA basis for each attention head
(\S\ref{app:fitting}), followed by an \emph{optimisation phase} in which
the per-head weight vectors are refined as free parameters via gradient
descent to maximise zero-shot classification accuracy (\S\ref{app:optim}).
The forward-pass reweighting applied at inference time is described in
\S\ref{app:forward_rw}.

Throughout this section I assume batch size $B = 1$ for notational
clarity; all operations extend straightforwardly to $B > 1$ by treating
the batch axis independently. I use the index conventions of
Appendix~\ref{app:architecture}: $\ell \in \{0,1,2,3\}$ is the stage
index, $b \in \{1,\ldots,B_\ell\}$ is the block index within stage
$\ell$, and $h \in \{1,\ldots,H_\ell\}$ is the head index. The
per-head feature dimension is $d_h = 24$ (constant across all stages),
$N_w^\ell = S_\ell^2 / M$ is the number of attention windows at stage
$\ell$, and $M = 64$ is the number of tokens per window. All symbols are
collected in Table~\ref{tab:notation}.

% ------------------------------------------------------------------
\subsection{Architecture and Module Injection}
\label{app:arch_injection}
% ------------------------------------------------------------------

\subsubsection{Spectral reweighting layers.}
For each target stage $\ell \in \mathcal{L}$, block $b$, and head $h$,
RD$^*$\xspace instantiates a \texttt{SpectralReweightingLayer} — a lightweight
\texttt{nn.Module} that carries two non-trainable buffers,
$\boldsymbol{\Phi}_{\ell,b,h} \in \mathbb{R}^{d_h \times d_h}$ and
$\boldsymbol{\mu}_{\ell,b,h} \in \mathbb{R}^{d_h}$, and a single
learnable parameter vector
$\boldsymbol{\lambda}_{\ell,b,h} \in \mathbb{R}^{d_h}$.
All such modules are collected in a nested \texttt{nn.ModuleDict}
indexed by $(\ell, b, h)$, constructed once during model initialisation
in \texttt{\_build\_spectral\_layers} from the stage dimensions and head
counts of the underlying HTS-AT configuration; they persist in the
model's \texttt{state\_dict} across saves and loads.
Each module exposes two methods: \texttt{fit\_pca}, which populates
$\boldsymbol{\Phi}_{\ell,b,h}$ and $\boldsymbol{\mu}_{\ell,b,h}$ from
data and initialises $\boldsymbol{\lambda}_{\ell,b,h}$
(\S\ref{app:fitting}); and \texttt{forward}, which applies the
RD$^*$\xspace transform (\S\ref{app:forward_rw}).

Figure~\ref{fig:sw-spectral} summarises the full lifecycle of a
\texttt{SpectralReweightingLayer}: the PCA fitting pass accumulates
$N_{\mathrm{clips}}$ training samples to derive
$\boldsymbol{\mu}_{\ell,b,h}$, $\boldsymbol{\Phi}_{\ell,b,h}$, and the
initial weight vector $\boldsymbol{\lambda}^{(0)}_{\ell,b,h}$
(Eqs.~\eqref{eq:k_threshold}--\eqref{eq:lambda_init}); the optimisation
phase then refines $\boldsymbol{\lambda}_{\ell,b,h}$ by minimising
$\mathcal{J}(\boldsymbol{\lambda})$ (Eq.~\eqref{eq:optim_obj}); at
inference time the four-step spectral reweighting transform
(Eqs.~\eqref{eq:rd_reweight-1}--\eqref{eq:rd_star}) is applied to each
incoming $\mathbf{H}_{\ell,b,h}$.

\begin{figure*}[h]
    \centering
    \includegraphics[width=\linewidth]{images/sw-mha.spectral.drawio.png}
    \caption{%
        Internal structure of a \texttt{SpectralReweightingLayer} for
        head $h$ of block $(\ell, b)$.
        \textbf{Right} (\emph{fitting pass}): $N_{\mathrm{clips}}$
        per-head activation matrices are concatenated to form the
        fitting matrix $\widetilde{\mathbf{R}}_{\ell,b,h}$
        (Eq.~\eqref{eq:fitting_matrix}), from which the empirical mean
        $\boldsymbol{\mu}_{\ell,b,h}$ and PCA basis
        $\boldsymbol{\Phi}_{\ell,b,h}$ are estimated
        (Eq.~\eqref{eq:pca_decomp}), and the initial weight vector
        $\boldsymbol{\lambda}^{(0)}_{\ell,b,h}$ is set via the
        variance-threshold criterion
        (Eqs.~\eqref{eq:k_threshold}--\eqref{eq:lambda_init}).
        \textbf{Centre} (\emph{spectral reweighting}): at inference
        time the four-step transform centres, projects onto the PCA
        basis, rescales per component, and reconstructs each head
        tensor (Eqs.~\eqref{eq:rd_reweight-1}--\eqref{eq:rd_star}).
        \textbf{Bottom-right} (\emph{optimisation}): the weight vector
        $\boldsymbol{\lambda}_{\ell,b,h}$ is refined by minimising
        $\mathcal{J}$ on additional training samples
        (\S\ref{app:optim}).
    }
    \label{fig:sw-spectral}
\end{figure*}

\subsubsection{Module injection via forward hooks.}
RD$^*$\xspace injects its reweighting through permanent PyTorch forward hooks.
For each target block $(\ell, b)$ with $\ell \in \mathcal{L}$, a
dedicated \texttt{AttentionHook} object is registered once on the
corresponding \texttt{WindowAttention} module at model construction via
\texttt{register\_forward\_hook} and remains attached throughout the
entire model lifetime — no registration or removal occurs at inference
time. The hook is stateless: it holds only a reference to the
corresponding slice \texttt{spectral\_layers}$[\ell][b]$ of the module
dict, and operates in one of two modes controlled by a
\texttt{collect\_for\_fitting} flag.
In \emph{collection mode}, it accumulates the raw per-head tensors
$\mathbf{H}_{\ell,b,h}$ without modifying the forward-pass output,
feeding the fitting pass of \S\ref{app:fitting}.
In \emph{reweighting mode}, it replaces $\mathbf{H}_{\ell,b,h}$ with
the reweighted output $\mathbf{H}^*_{\ell,b,h}$
(Eq.~\eqref{eq:reweighted_head}) and immediately re-applies the output
projection $W^O_{\ell,b}$ and dropout \texttt{proj\_drop} within the
hook itself before returning the modified output\footnote{%
    Since the hook receives the output of \texttt{WindowAttention} rather
    than the intermediate pre-projection activations, the per-head tensors
    $\mathbf{H}_{\ell,b,h}$ are recovered by re-running the fused QKV
    projection $W^{QKV}_{\ell,b}$ on the hook's \texttt{inputs[0]} and
    recomputing $\mathbf{H}_{\ell,b,h} = \boldsymbol{\alpha}_{\ell,b,h}
    \mathbf{V}_{\ell,b,h}$ within the hook itself. This incurs a second
    QKV computation per forward pass at the target blocks, which is
    negligible given that $\boldsymbol{\Phi}_{\ell,b,h}$ and
    $\boldsymbol{\lambda}_{\ell,b,h}$ operate in the low-dimensional
    space $\mathbb{R}^{d_h}$ with $d_h = 24$.%
}; this is necessary
because returning a modified tensor from the hook bypasses the
projection that would otherwise follow in the original
\texttt{WindowAttention} forward.
No structural modification of the original model is performed: all
pretrained parameters — the fused QKV projection $W^{QKV}_{\ell,b}$,
the output projection $W^O_{\ell,b}$ with bias $\mathbf{b}^O_{\ell,b}$,
and the relative position bias table $\mathbf{B}_{\ell,b,h}$ — remain
exactly as loaded from the checkpoint.

Figure~\ref{fig:sw-mha-window} illustrates the resulting data flow for
a single target block $(\ell, b)$: the \texttt{WindowAttention} module
produces one tensor $\mathbf{H}_{\ell,b,h}$ per head, each of which is
intercepted by the corresponding hook and processed independently by
the \texttt{SpectralReweightingLayer} before the outputs are reassembled
and passed to the output projection $W^O_{\ell,b}$.

\begin{figure*}[h]
    \centering
    \includegraphics[width=0.9\linewidth]{images/sw-mha.window.drawio.png}
    \caption{%
        Reweighting pipeline for a single target block $(\ell, b)$.
        The \texttt{WindowAttention} module computes the raw per-head
        tensors $\mathbf{H}_{\ell,b,h} \in \mathbb{R}^{N_w^\ell \times M \times d_h}$
        via the standard scaled dot-product attention with relative
        position bias $\mathbf{B}_{\ell,b,h}$.
        PyTorch \texttt{AttentionHooks} intercept each
        $\mathbf{H}_{\ell,b,h}$ and route it through the corresponding
        \texttt{SpectralReweightingLayer}; the reweighted outputs
        $\mathbf{H}^*_{\ell,b,h}$ are then stacked, reshaped, and
        projected by $W^O_{\ell,b}$ to yield the block output
        $\mathbf{Z}^{(\ell,b)}_{\mathrm{out}} \in
        \mathbb{R}^{N_w^\ell \times M \times D_\ell}$.
    }
    \label{fig:sw-mha-window}
\end{figure*}

% ------------------------------------------------------------------
\subsection{Fitting Pass: PCA Basis Estimation}
\label{app:fitting}
% ------------------------------------------------------------------

\subsubsection{Data collection.}
The fitting pass is run with gradients disabled and the model in
evaluation mode. For each audio clip (up to $N_{\max}$ clips), a
forward pass is executed with data collection enabled. The hook
registered at block $(\ell, b)$ intercepts, for every head $h$, the
tensor $\mathbf{H}_{\ell,b,h} \in \mathbb{R}^{N_w^\ell \times M \times d_h}$
and flattens it along the spatial axes to obtain a matrix of shape
$\mathbb{R}^{(N_w^\ell \cdot M) \times d_h}$, which is appended to an
accumulation buffer for the triplet $(\ell, b, h)$. After all clips
have been processed, the per-clip contributions are stacked to form the
\emph{fitting matrix}
\begin{equation}
    \widetilde{\mathbf{R}}_{\ell,b,h}
    \;\in\; \mathbb{R}^{N_{\mathrm{fit}} \times d_h},
    \qquad
    N_{\mathrm{fit}}
    \;=\; N_{\mathrm{clips}} \cdot N_w^\ell \cdot M,
    \label{eq:fitting_matrix}
\end{equation}
where $N_{\mathrm{clips}} \le N_{\max}$ is the number of processed clips.

\subsubsection{PCA decomposition.}
Standard PCA is applied to $\widetilde{\mathbf{R}}_{\ell,b,h}$.
The empirical mean
\begin{equation}
    \boldsymbol{\mu}_{\ell,b,h}
    \;=\; \frac{1}{N_{\mathrm{fit}}}
          \sum_{i=1}^{N_{\mathrm{fit}}}
          \widetilde{\mathbf{R}}_{\ell,b,h}[i,:]
    \;\in\; \mathbb{R}^{d_h}
\end{equation}
is computed and subtracted, and the centred empirical covariance is
decomposed as
\begin{align}
    \mathbf{C}_{\ell,b,h}
    &= \frac{1}{N_{\mathrm{fit}} - 1}
       \Bigl(\widetilde{\mathbf{R}}_{\ell,b,h}
             - \mathbf{1}\boldsymbol{\mu}_{\ell,b,h}^\top\Bigr)^\top
       \Bigl(\widetilde{\mathbf{R}}_{\ell,b,h}
             - \mathbf{1}\boldsymbol{\mu}_{\ell,b,h}^\top\Bigr) \notag \\
    &= \boldsymbol{\Phi}_{\ell,b,h}\,
       \operatorname{diag}(\lambda^{\mathrm{ev}}_1, \ldots,
                            \lambda^{\mathrm{ev}}_{d_h})\,
       \boldsymbol{\Phi}_{\ell,b,h}^\top,
    \label{eq:pca_decomp}
       \\
    &\text{with }\lambda^{\mathrm{ev}}_1 \ge \cdots \ge
    \lambda^{\mathrm{ev}}_{d_h} \ge 0,
\end{align}
where $\boldsymbol{\Phi}_{\ell,b,h} \in \mathbb{R}^{d_h \times d_h}$ is
the orthogonal matrix of eigenvectors (columns ordered by decreasing
eigenvalue) and $\lambda^{\mathrm{ev}}_j$ denotes the $j$-th eigenvalue
of $\mathbf{C}_{\ell,b,h}$. I use the superscript ``ev'' throughout to
distinguish these fixed quantities from the trainable weight vector
$\boldsymbol{\lambda}_{\ell,b,h}$.
Both $\boldsymbol{\Phi}_{\ell,b,h}$ and $\boldsymbol{\mu}_{\ell,b,h}$
are stored as non-trainable buffers; the accumulation buffers
$\widetilde{\mathbf{R}}_{\ell,b,h}$ are then cleared to free memory.

\subsubsection{Selection of the task-relevant subspace.}
The number of principal components retained for each head is determined
by a variance-threshold criterion. Define the normalised eigenvalue
shares and their cumulative sum as
\begin{equation}
    \rho_k
    = \frac{\lambda^{\mathrm{ev}}_k}{\sum_{j=1}^{d_h} \lambda^{\mathrm{ev}}_j},
    \qquad
    S_k = \sum_{j=1}^{k} \rho_j,
\end{equation}
so that $S_{d_h} = 1$ by construction. The retained dimensionality is
then the smallest $k$ sufficient to explain a fraction $\rho_{\mathrm{var}}$
of the total variance:
\begin{equation}
    k_{\ell,b,h}
    = \min\!\left\{
        k \in \{1, \ldots, d_h\}
        \;\middle|\;
        S_k \ge \rho_{\mathrm{var}}
      \right\}.
    \label{eq:k_threshold}
\end{equation}
Because $d_h = 24$ is small and constant, this threshold is evaluated
exactly over all components without approximation. The default value
$\rho_{\mathrm{var}} = 0.95$ is used throughout.

\subsubsection{Initialisation of $\boldsymbol{\lambda}_{\ell,b,h}$.}
For each head $(\ell,b,h)$, the index $k_{\ell,b,h}$ defined in
Eq.~\eqref{eq:k_threshold} identifies the smallest number of principal
components that together explain at least a fraction $\rho_{\mathrm{var}}$
of the total variance, providing a per-head estimate of the effective
dimensionality of the task-relevant subspace.
The learnable weight vector
$\boldsymbol{\lambda}_{\ell,b,h} \in \mathbb{R}^{d_h}$
is initialised component-wise as
\begin{equation}
    \lambda_j^{(0)}
    \;=\; \begin{cases}
        1 & j \le k_{\ell,b,h}, \\
        0 & j > k_{\ell,b,h},
    \end{cases}
    \label{eq:lambda_init}
\end{equation}
corresponding to the identity map restricted to the task-relevant
subspace and complete suppression of the remaining noise components.
At this initialisation point, RD$^*$\xspace therefore acts as a
projection-and-reconstruction operator that retains the top-$k_{\ell,b,h}$
PCA directions unchanged and zeros out the remainder.

% ------------------------------------------------------------------
\subsection{Optimisation of $\boldsymbol{\lambda}$ via Zero-Shot Accuracy}
\label{app:optim}
% ------------------------------------------------------------------

After the fitting pass has fixed the PCA bases
$\{\boldsymbol{\Phi}_{\ell,b,h}\}$ and initialised
$\{\boldsymbol{\lambda}_{\ell,b,h}\}$ via Eq.~\eqref{eq:lambda_init},
the second phase optimises the weight vectors to maximise zero-shot
classification accuracy on a held-out validation set.

\subsubsection{Parameter isolation.}
All model parameters except the weight vectors
$\{\boldsymbol{\lambda}_{\ell,b,h}\}_{\ell \in \mathcal{L},\,b,\,h}$
are frozen by setting \texttt{requires\_grad = False}. The only degrees
of freedom during optimisation are therefore the
$\sum_{\ell \in \mathcal{L}} B_\ell \cdot H_\ell$ weight vectors, each
of dimension $d_h = 24$, that control the per-component gain of each
attention head in the target stages $\mathcal{L}$.

\subsubsection{Objective.}
Given a set of audio clips with ground-truth class labels
$y \in \{1,\ldots,C\}$ and pre-computed $\ell_2$-normalised text
embeddings $\mathbf{t}_c \in \mathbb{R}^{d_{\mathrm{proj}}}$ for each
class $c$, the optimisation objective is the cross-entropy loss over
zero-shot logits:
\begin{equation}
    \mathcal{J}(\boldsymbol{\lambda})
    \;=\;
    -\,\mathbb{E}_{(x,\,y)}\!\left[
        \log \frac{
            \exp\!\bigl(\hat{\mathbf{a}}(x)^\top \mathbf{t}_y\bigr)
        }{
            \displaystyle\sum_{c=1}^{C}
            \exp\!\bigl(\hat{\mathbf{a}}(x)^\top \mathbf{t}_c\bigr)
        }
    \right],
    \label{eq:optim_obj}
\end{equation}
where $\hat{\mathbf{a}}(x) \in \mathbb{R}^{d_{\mathrm{proj}}}$ is the
$\ell_2$-normalised audio embedding produced by $P$ applied to the
spatially averaged Stage-3 output of HTS-AT after the RD$^*$\xspace
transforms have been applied (cf.\ Table~\ref{tab:notation}).
Minimising $\mathcal{J}$ is equivalent to maximising zero-shot
classification accuracy in expectation.

\subsubsection{Optimiser and schedule.}
The weight vectors $\{\boldsymbol{\lambda}_{\ell,b,h}\}$ are updated
with AdamW~\cite{adamw} at learning rate $\eta$ and
weight decay $\gamma$; all hyperparameter values are reported in
Table~\ref{tab:notation}. The CLAP audio projection head
$P : \mathbb{R}^{D_3} \to \mathbb{R}^{d_{\mathrm{proj}}}$ is kept
frozen but is included in the computational graph so that gradients
flow from the similarity space back to
$\boldsymbol{\lambda}_{\ell,b,h}$.

\subsubsection{Early stopping and best-$\boldsymbol{\lambda}$ restoration.}
At the end of each epoch, zero-shot accuracy on the validation set is
evaluated. A snapshot of the current
$\{\boldsymbol{\lambda}_{\ell,b,h}\}$ is saved whenever a new accuracy
maximum is achieved. Training halts early if no improvement is observed
for $p$ consecutive epochs; at termination, the best snapshot is
restored, guaranteeing that the final weight vectors correspond to the
configuration achieving the highest observed validation accuracy.

\subsubsection{Hyperparameter summary.}
Table~\ref{tab:notation} collects all hyperparameters introduced
by RD$^*$\xspace together with the default values used in all experiments.

% ------------------------------------------------------------------
\subsection{Inference: Forward Pass with Spectral Reweighting}
\label{app:forward_rw}
% ------------------------------------------------------------------
At inference time, the hook at block $(\ell, b)$ intercepts the raw head
outputs and applies the following sequence of operations independently
for each head $h$.

\textbf{Step 0 — Flattening.}
The raw head output, originally shaped as
$\mathbb{R}^{N_w^\ell \times M \times d_h}$, is first flattened along
the first two axes:
\begin{equation}
    \mathbf{H}_{\ell,b,h}
\;=\; \texttt{reshape}(\mathbf{H}_{\ell,b,h})
\;\in\; \mathbb{R}^{(N_w^\ell \cdot M) \times d_h},
\label{eq:rd_flatten}
\end{equation}
so that all subsequent operations act on a 2-D matrix of token
representations.

\textbf{Step 1 — Centering.}
\begin{equation}
    \widetilde{\mathbf{H}}_{\ell,b,h}
\;=\; \mathbf{H}_{\ell,b,h} - \boldsymbol{\mu}_{\ell,b,h}
\;\in\; \mathbb{R}^{(N_w^\ell \cdot M) \times d_h},
\label{eq:rd_reweight-1}
\end{equation}
where $\boldsymbol{\mu}_{\ell,b,h} \in \mathbb{R}^{d_h}$ is broadcast
over the first axis.

\textbf{Step 2 — Projection onto the full PCA basis.}
\begin{equation}
    \mathbf{Z}_{\ell,b,h}
\;=\; \widetilde{\mathbf{H}}_{\ell,b,h}\,\boldsymbol{\Phi}_{\ell,b,h}
\;\in\; \mathbb{R}^{(N_w^\ell \cdot M) \times d_h}.
\label{eq:rd_reweight-2}
\end{equation}
Unlike fixed-weight variants that truncate to $k_{\ell,b,h} < d_h$
components, RD$^*$\xspace projects onto the \emph{full} basis
$\boldsymbol{\Phi}_{\ell,b,h} \in \mathbb{R}^{d_h \times d_h}$,
delegating effective truncation to the learned weights.

\textbf{Step 3 — Per-component rescaling.}
\begin{equation}
    \widehat{\mathbf{Z}}_{\ell,b,h}
\;=\; \mathbf{Z}_{\ell,b,h}
\odot \boldsymbol{\lambda}_{\ell,b,h}^\top
\;\in\; \mathbb{R}^{(N_w^\ell \cdot M) \times d_h},
\label{eq:rd_reweight-3}
\end{equation}
where $\boldsymbol{\lambda}_{\ell,b,h}^\top \in \mathbb{R}^{d_h}$ is
broadcast over the first axis. Each entry $\lambda_j$ can amplify
($|\lambda_j|>1$), attenuate ($|\lambda_j|\in(0,1)$), suppress
($\lambda_j=0$), or invert ($\lambda_j<0$) the corresponding principal
component.

\textbf{Step 4 — Reconstruction in the original space.}
\begin{equation}
    \mathbf{H}^*_{\ell,b,h}
\;=\; \widehat{\mathbf{Z}}_{\ell,b,h}\,\boldsymbol{\Phi}_{\ell,b,h}^\top
\;\in\; \mathbb{R}^{(N_w^\ell \cdot M) \times d_h}.
\label{eq:reweighted_head}
\end{equation}
The empirical mean $\boldsymbol{\mu}_{\ell,b,h}$ is \emph{not} restored
after reconstruction, as it is already absorbed by the learnable bias
$\mathbf{b}^O_{\ell,b}$ of the output projection $W^O_{\ell,b}$
(cf.\ Eq.~\eqref{eq:wmsa_out}).

\textbf{Step 5 — Reshaping.}
Before being passed to the output projection, $\mathbf{H}^*_{\ell,b,h}$
is reshaped back to the original spatial layout:
\begin{equation}
    \mathbf{H}^*_{\ell,b,h}
\;\leftarrow\;
    \texttt{reshape}(\mathbf{H}^*_{\ell,b,h})
\;\in\; \mathbb{R}^{N_w^\ell \times M \times d_h}.
\label{eq:rd_reshape}
\end{equation}

Combining Steps~1--4, the full RD$^*$\xspace transform (on the flattened
representation) reads:
\begin{equation}
\begin{aligned}
    &\mathrm{RD}^*\!\left(\mathbf{H}_{\ell,b,h},\,
                          \boldsymbol{\lambda}_{\ell,b,h}\right) \;\in\; \mathbb{R}^{(N_w^\ell \cdot M) \times d_h}
    \;=\;\\
    &=\left(\mathbf{H}_{\ell,b,h} - \boldsymbol{\mu}_{\ell,b,h}\right)
    \boldsymbol{\Phi}_{\ell,b,h}\,
    \operatorname{diag}(\boldsymbol{\lambda}_{\ell,b,h})\,
    \boldsymbol{\Phi}_{\ell,b,h}^\top.
\end{aligned}
\label{eq:rd_star}
\end{equation}
The reshaped $\mathbf{H}^*_{\ell,b,h}$ replaces $\mathbf{H}_{\ell,b,h}$
in Eq.~\eqref{eq:wmsa_out}, so the output projection $W^O_{\ell,b}$
and the residual-stream update of Eq.~\eqref{eq:residual_attn} proceed
unchanged. During optimisation, gradients of any downstream loss flow
through $\mathrm{RD}^*$ and update only
$\boldsymbol{\lambda}_{\ell,b,h}$; the basis
$\boldsymbol{\Phi}_{\ell,b,h}$, the mean $\boldsymbol{\mu}_{\ell,b,h}$,
and all original model parameters are held fixed.